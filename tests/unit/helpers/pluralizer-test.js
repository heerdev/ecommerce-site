import { pluralizer } from 'ecommerce-store/helpers/pluralizer';
import { module, test } from 'qunit';

module('Unit | Helper | pluralizer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pluralizer([42]);
  assert.ok(result);
});
